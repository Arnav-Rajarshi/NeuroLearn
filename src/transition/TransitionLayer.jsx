import NeuralUplink from './NeuralUplink'

/**
 * TransitionLayer
 * Rendered once at the app root — sits above all page content.
 * The overlay is visibility-controlled by TransitionContext.
 */
export default function TransitionLayer() {
  return <NeuralUplink />
}
